# MusicCenter 产品需求文档(PRD)

## 1. 产品概述

**MusicCenter**:基于 Flutter 的云盘音乐播放器。用户授权接入云盘(阿里云盘首发,百度云盘开发中),选定云盘中的一个文件夹后,自动加载该文件夹内(含子目录)的音乐文件与歌词(.lrc),构建本地音乐库并播放。

**目标平台**:
- 首发:Windows 桌面
- 后期:Android、iOS

**核心价值**:无需下载/迁移文件,云盘即音乐库;支持边下边播,节省等待时间。

---

## 2. 版本路线

| 阶段 | 平台 | 内容 |
|------|------|------|
| 阶段一 MVP | Windows | 阿里云盘授权 → 文件夹选择 → 音乐列表 → 播放 + 歌词 → 边下边播缓存 |
| 阶段二 | Windows | 播放模式增强、收藏/历史、缓存管理、主题、在线播放模式完善 |
| 阶段三 | Windows | 百度云盘接入(开发中) |
| 阶段四 | Android / iOS | 后台播放、锁屏媒体控制、响应式 UI、音频焦点 |
| 阶段五 | 三端 | 在线歌词搜索、全局快捷键、桌面通知等增强 |

---

## 3. 云盘接入(核心模块,与 UI 完全解耦)

### 3.1 优先级
- **P0(首发):阿里云盘 Open API**
- P1(阶段三):百度云盘(PCS API,开发中,接口层预留扩展点)

### 3.2 需求明细
| 需求 | 说明 | 优先级 |
|------|------|--------|
| 统一接口抽象 | 定义 `CloudDrive` 接口(授权/目录/文件列表/下载地址),阿里与百度各实现一份,UI 只依赖抽象 | P0 |
| 阿里云盘授权 | OAuth 2.0 授权码流程,获取 access_token / refresh_token | P0 |
| Token 管理 | 本地持久化、自动刷新、失效处理、多账号、账号切换 | P0 |
| 目录浏览 | 根目录 + 文件夹树浏览,选择目标文件夹 | P0 |
| 文件列表 | 列出文件夹内文件(含子目录递归),获取元信息(名称/大小/类型/修改时间) | P0 |
| 下载地址获取 | 获取文件可播放的 download_url(阿里云盘有下载限流,需处理) | P0 |
| 限流与重试 | 限流退避、网络错误重试、断线恢复 | P0 |
| 百度云预留 | 接口层可扩展,后续实现不侵入 UI | P1 |

### 3.3 风险点
- 阿里云盘 download_url 有效期与限流策略需实测,必要时转为"先缓存再播"
- 云盘 API 审核要求(阿里云盘开放平台需应用审核),提前准备材料

---

## 4. 音乐库

### 4.1 来源管理(歌曲来源 / 歌词来源)
| 需求 | 说明 | 优先级 |
|------|------|--------|
| 默认来源 | 绑定云盘并选中文件夹后,该文件夹**自动同时作为【歌曲来源】和【歌词来源】**(默认同目录场景) | P0 |
| 添加歌曲来源 | 设置中可添加更多云盘文件夹作为歌曲来源;多来源合并,按文件 ID/路径去重 | P1 |
| 添加歌词来源 | 设置中可添加更多云盘文件夹作为歌词来源(音乐与歌词不同目录场景) | P1 |
| 来源管理 | 查看/移除来源;歌词来源列表顺序即歌词匹配优先级 | P1 |
| 位置记忆 | 记住各来源配置,启动自动恢复 | P0 |

### 4.2 歌曲加载
| 需求 | 说明 | 优先级 |
|------|------|--------|
| 扫描加载 | 遍历所有歌曲来源文件夹(含子目录),收集音频文件,并按其来源关联歌词 | P0 |
| 音频格式 | mp3 / flac / wav / m4a / ogg / aac | P0 |
| 元数据解析 | ID3v2(mp3)、FLAC 标签 → 曲名/歌手/专辑/封面;失败时用文件名兜底 | P0 |
| 列表功能 | 排序(曲名/歌手/专辑/时长)、搜索、筛选 | P1 |
| 收藏/历史 | 收藏列表、播放历史、最近播放,存本地数据库 | P1 |

---

## 5. 播放核心

### 5.1 基础控制
播放 / 暂停 / 上一首 / 下一首 / 进度拖动 / 音量。

### 5.2 播放模式
顺序、列表循环、单曲循环、随机。

### 5.3 播放策略(双模式,用户可切换)
| 模式 | 行为 | 说明 |
|------|------|------|
| **边下边播(默认)** | 流式播放 + 分块缓存到本地 | 缓存命中后秒开;断网时可播放已缓存部分 |
| **在线播放(不缓存)** | 直接流式播放,不落盘 | 关闭即失效,断网即中断,节省本地空间 |

- 播放策略为**全局设置**,切换后生效
- 当前歌曲的缓存状态需在 UI 可见(如"已缓存 / 缓存中 / 在线")

### 5.4 缓存管理(针对边下边播模式)
| 需求 | 说明 | 优先级 |
|------|------|--------|
| 缓存目录 | 应用私有目录,按歌曲分文件 | P0 |
| 容量上限 | 可配置(如 5GB),超限自动清理 | P1 |
| 清理策略 | LRU(最近最少使用)优先清理 | P1 |
| 缓存状态查看 | 展示缓存占用、歌曲缓存明细、手动清理 | P1 |

### 5.5 队列
- 从任意歌曲开始播放
- 队列管理:插入、移除、排序、清空

---

## 6. 歌词

### 6.1 歌词来源匹配(按优先级链,命中即止)
对每首歌曲,按以下歌词来源顺序查找:

| 优先级 | 来源 | 说明 | 版本 |
|--------|------|------|------|
| 1 | 歌词来源列表匹配 | 遍历 4.1 中所有歌词来源(默认包含选中文件夹,可添加额外来源),对每个来源按 镜像路径 → 文件名 顺序匹配 | MVP(仅默认来源)/ 后期(额外来源) |
| 2 | 内嵌歌词 | mp3 ID3 USLT/SYLT 标签、FLAC 内嵌歌词 | 后期 |
| 3 | 在线歌词 | 网络搜索匹配 | 后期 |

**歌词来源匹配规则**(覆盖同目录与分目录场景):
- 默认来源即歌曲选中文件夹:**同目录同名 .lrc**(零配置,最可靠)
- 额外歌词来源(音乐与歌词不同目录时):
  1. **镜像路径匹配**(推荐,最准确):该来源下存在与音乐文件**相同相对路径**的同名 .lrc,如 `音乐/专辑A/歌.mp3` ↔ `歌词/专辑A/歌.lrc`
  2. **文件名匹配**(兜底):在来源内按文件名查找同名 .lrc;重名时结合歌手名辅助判断,仍无法确定则取第一个并提示
- 文件名归一化:忽略大小写、首尾空格、全半角差异,提升命中率
- 全部来源匹配失败则进入下一优先级(内嵌歌词 / 在线歌词 / 无歌词兜底)

> 歌词模块设计为"歌词源"抽象(`LyricsSource`),UI 只依赖匹配结果;后续新增来源不侵入播放页。

### 6.2 需求明细
| 需求 | 说明 | 优先级 |
|------|------|--------|
| .lrc 解析 | 时间戳 + 文本解析,支持逐行同步 | P0 |
| 编码识别 | UTF-8 / GBK 自动识别(中文歌词常见) | P0 |
| 同步滚动 | 当前行高亮,自动跟随;可拖动歌词调整进度 | P0 |
| 无歌词兜底 | 展示封面 + 曲目信息 | P0 |
| 在线歌词搜索 | 网络匹配歌词(可选,后期) | P2 |

---

## 7. UI 设计

| 模块 | 说明 | 优先级 |
|------|------|--------|
| 桌面三栏布局 | 左侧边栏(云盘文件夹/收藏/历史)+ 中央歌曲列表 + 底部播放栏 | P0 |
| 播放页 | 大封面 + 歌词视图,支持沉浸 | P0 |
| 移动端布局 | 底部导航 + 底部播放栏,响应式适配 | P1 |
| 主题 | 深色 / 浅色,跟随系统 | P1 |
| 国际化 | 中文优先,预留 i18n | P2 |

---

## 8. 平台差异需求

### 8.1 Windows(首发)
- 系统托盘、最小化到托盘
- 窗口记忆(大小/位置)
- 全局快捷键(可选)、桌面通知(可选)

### 8.2 Android / iOS(后期)
- **后台播放**(关键硬需求)
- 锁屏 / 通知栏媒体控制(上一首/下一首/进度)
- 音频焦点处理(来电、其他应用)
- Android 电池优化白名单引导

---

## 9. 非功能性需求

- **性能**:千首以上列表流畅滚动,图片缓存,内存控制
- **网络**:弱网、限流、断线重连、失败重试
- **安全**:Token 加密存储(Windows 系统凭据 / 移动端 Keystore、Keychain)
- **崩溃监控**:错误日志采集与上报通道(可选)
- **可维护性**:云盘接口、播放器、歌词解析均为独立模块,便于后续扩展

---

## 10. 技术选型(建议,实施前验证)

| 场景 | 候选 | 备注 |
|------|------|------|
| 音频播放 | media_kit / audioplayers / just_audio | 需实测三端支持与 flac 解码;media_kit 基于 libmpv,格式覆盖广 |
| 元数据解析 | audio_metadata_reader / taglib | 读取 ID3v2 / FLAC 标签 |
| 网络请求 | dio | 支持限流重试、下载进度 |
| 本地存储 | drift / hive | 数据库存元数据/收藏/缓存索引;Token 用安全存储 |
| 状态管理 | riverpod / provider / bloc | 推荐 riverpod(编译期安全) |
| 权限 | permission_handler | 移动端网络/存储权限 |

> 选型原则:优先三端均稳定支持的库;播放器底层支持 Windows 是首发前提。

---

## 11. 里程碑(建议)

1. **M1 阿里云盘接入**:授权、Token 管理、目录浏览、文件列表 → 可在 Windows 浏览云盘文件夹
2. **M2 播放闭环**:选定文件夹 → 加载列表 → 边下边播播放 + 进度控制 → 可完整听歌
3. **M3 歌词**:.lrc 解析 + 同步滚动播放页
4. **M4 体验完善**:在线播放模式、缓存管理、收藏/历史、主题
5. **M5 百度云盘接入**
6. **M6 移动端**:后台播放、锁屏控制、响应式 UI
